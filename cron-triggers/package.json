{
  "name": "cron-triggers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.8.5"
  },
  "devDependencies": {
    "@types/node": "^24.0.14",
    "wrangler": "^4.4.0"
  }
}
